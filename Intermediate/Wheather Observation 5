SELECT 
    CITY, LENGTH(CITY) 
FROM 
    STATION
ORDER BY 
    LENGTH(CITY), CITY 
LIMIT 1;

/* We can write two separate queries to get the desired output */

SELECT 
    CITY, LENGTH(CITY) 
FROM 
    STATION
ORDER BY 
    LENGTH(CITY) DESC, CITY 
LIMIT 1;
